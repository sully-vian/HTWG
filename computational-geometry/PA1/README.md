# Practical Assignment 1: Convex hull computation

- [ ] Graham's Scan
- [ ] Jarvis' March

## Commands

efore running these commands, go in the `CompGeoTemplate/` drectory.

To generate the `Makefile` files run:

```bash
qmake6 CompGeoTemplate.pro
```

To build the project run:

```bash
make
```

To start the project run:

```bash
./debug/CompGeoTemplate
```

