# computational-geometry

Lab sessions of my computational geometry class

Possible conflicts between different QT versions can prevent the `qmake` command from working. Use `qmake6` if available

When decompressing the template's archive from Moddle, the directories' permissions might be broken. To fix, run:

```bash
$ find CompGeoTemplate -type d -exec chmod 755 {} +
```

